import {
    MODAL_CREATE_ROOM,
} from './actions'

export const reducer = (state, action) => {

    switch (action.type) {
        case MODAL_CREATE_ROOM: {
            return {

            }
        }
    }

}