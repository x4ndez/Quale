const resolvers = {

    Query: {
        users: async () => {
            return;
        }
    },

    Mutation: {
        addUser: async () => {
            return;
        }
    }

};

module.exports = resolvers;